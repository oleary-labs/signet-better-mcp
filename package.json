{
  "name": "@oleary/signet-better-mcp",
  "version": "0.0.1",
  "private": true,
  "description": "MCP server for Signet — exposes threshold-signing operations to AI agents over MCP, with OAuth via Better Auth and ZK-bound session auth against a Signet group",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@oleary-labs/signet-sdk": "^0.2.0",
    "better-auth": "^1.5.0",
    "fetch-to-node": "^2.1.0",
    "hono": "^4.6.13",
    "viem": "^2.21.55",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
